import React from 'react';

const Nav = () => {
    const links = [
        {
            name: "About",
            path: "/about"
        }
    ]
    
    return (
        <nav>
            
        </nav>
    );
};

export default Nav;